name: Workflow for Codecov openrtm-lua unit-test
on: [push, pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Lua 5.3
        run: apt -y install lua5.3 luarocks
